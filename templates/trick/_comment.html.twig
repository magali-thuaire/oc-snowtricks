<li class="mb-4">
    <div class="row align-items-center">
        <div class="col-2 text-start">
            <img class="trick__user__avatar rounded-circle" src="{{ asset('build/images/avatar.jpg') }}" alt="avatar">
            <p class="text-muted m-0">{{ comment.commentedBy.username }}</p>
        </div>
        <p class="col-8 m-0 text-start ps-4 ps-md-1">{{ comment.content }}</p>
    </div>
</li>
